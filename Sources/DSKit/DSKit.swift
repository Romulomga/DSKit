import Foundation

/// Umbrella namespace with package metadata. The real public surface lives
/// in the individual `DS*` types under `Tokens`, `Theme`, `Components`.
public enum DSKit {
    public static let version = "0.1.0"
}
